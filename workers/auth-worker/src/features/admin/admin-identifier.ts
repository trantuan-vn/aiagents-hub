import { getPrimaryAdminIdentifier } from '../../shared/admin-config';

/** @deprecated Import getPrimaryAdminIdentifier(env) — kept for gradual migration */
export const COMMISSION_ADMIN_IDENTIFIER = 'tuanta2021@gmail.com';

export { getPrimaryAdminIdentifier };
